# Supabase – same project as back office (Railway). Required for Vercel.
# Storefront reads products directly from Supabase; no API_URL needed.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Supabase – server only (Server Actions, e.g. createOrder). Required for Vercel.
# Never expose this to the client. Must match the back office Supabase project.
SUPABASE_SERVICE_ROLE_KEY=eyJ...
